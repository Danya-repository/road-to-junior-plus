describe('Guard reject service', () => {

    beforeEach(() => {

    })

    it('should must have active subscription after initialize', () => {

    })

    it('should must have not active subscription after deactivate', () => {

    })

    it('should process only navigation NavigationCancel and GuardRejected event', () => {

    })

    it('should redirect on MAIN page if user auth is authorized', () => {

    })

    it('should redirect on LOGIN page if user auth is not authorized', () => {

    })
})